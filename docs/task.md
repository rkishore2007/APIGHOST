# APIGhost Execution Tasks

- [x] Initialize Git repository and project structure
- [x] Setup `pyproject.toml` and install dependencies
- [x] Implement `Spec Parser` (`parser.py`) using `prance`
- [x] Implement `Chain Builder` (`chain_builder.py`) with Dual-Layer Resolution
- [x] Implement `Data Generator` (`generator.py`) with Three-Tier Resolution
- [x] Implement `Network Engine` (`executor.py`) with WAF resilience and LIFO teardown
- [x] Implement `Verdict Engine` (`verdict.py`) with Multi-Signal Jaccard Scoring
- [x] Integrate components into main CLI flow
- [x] Full integration test — CLI `chains` command verified end-to-end
- [ ] Verify against OWASP crAPI
